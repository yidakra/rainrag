# Copy this file to secrets/openai_api_key.txt and set your OpenAI API key.
# See https://platform.openai.com/docs for how to sign up and get an API key.
# This file is a template and is safe to check into version control.
#
# WARNING: do not commit the key. Ensure secrets/openai_api_key.txt (or **/openai_api_key.txt)
# is listed in .gitignore before creating your real file.
# Add secrets/openai_api_key.txt to .gitignore and set strict permissions (e.g., chmod 600) once created.
#
YOUR_OPENAI_API_KEY_HERE
# Reminder: ensure secrets/openai_api_key.txt is added to .gitignore before adding real key.
