# Copy this file to secrets/anthropic_api_key.txt and set your Anthropic Claude API key.
# See https://docs.anthropic.com for how to sign up and get an API key.
# This file is a template and is safe to check into version control.
#
# WARNING: do not commit the real key. Ensure secrets/anthropic_api_key.txt
# (or **/anthropic_api_key.txt) is in .gitignore before creating the real file.
# Add secrets/anthropic_api_key.txt to .gitignore and set strict permissions (e.g., chmod 600) once created.
#
YOUR_ANTHROPIC_API_KEY_HERE
# Reminder: verify secrets/anthropic_api_key.txt is in .gitignore before adding your real key.
