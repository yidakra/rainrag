# Copy this file to secrets/cohere_api_key.txt and paste your Cohere API key.
# See Cohere API key / authentication docs: https://cohere.com/docs/api/authentication
# This file is a template and is safe to check into version control.
#
# WARNING: do not commit the real key. Ensure **/cohere_api_key.txt is in .gitignore before creating the real file.
# Use **/cohere_api_key.txt to catch this filename in any subfolder (recommended for flexible project layouts).
# Use secrets/cohere_api_key.txt only when you are certain the key file will always live in the top-level secrets/ directory.
# Add the chosen pattern to .gitignore and set strict permissions on the real file (e.g., chmod 600) once created.
#
# REPLACE the line below with your real Cohere API key before using.
# Do NOT commit this file after replacement.
REPLACE_WITH_COHERE_API_KEY_DO_NOT_COMMIT
