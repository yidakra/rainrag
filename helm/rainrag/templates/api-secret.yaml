{{- if and .Values.api.enabled .Values.api.authToken }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rainrag.fullname" . }}-api-secret
  labels:
    {{- include "rainrag.api.labels" . | nindent 4 }}
type: Opaque
stringData:
  auth-token: {{ .Values.api.authToken | quote }}
{{- end }}
